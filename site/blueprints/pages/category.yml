title: Catégorie
icon: grid-bottom

options:
  changeSlug: true
  changeStatus: true
  delete: true

columns:
  - width: 2/3
    sections:
      projects:
        label: Projets
        type: pages
        template: project
        layout: cards
        size: small
        image:
          cover: true
        info: "{{ page.year }}"
        sortBy: title asc
  - width: 1/3
    fields:
      intro:
        label: Introduction
        type: textarea
        help: Optionnel
